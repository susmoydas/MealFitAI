import * as React from 'react';
import { Insets } from '@rn-primitives/types';
import { ScaledSize, LayoutRectangle } from 'react-native';

interface AugmentRefProps<T> {
    ref: React.Ref<T>;
    methods?: Record<string, (...args: any[]) => any>;
    deps?: any[];
}
/**
 * @deprecated Use useComposedRefs for new code. This hook remains only for
 * backwards compatibility with the older imperative ref augmentation pattern.
 */
declare function useAugmentedRef<T>({ ref, methods, deps }: AugmentRefProps<T>): React.RefObject<T | null>;

type UseRelativePositionArgs = Omit<GetContentStyleArgs, 'triggerPosition' | 'contentLayout' | 'dimensions'> & {
    triggerPosition: LayoutPosition | null;
    contentLayout: LayoutRectangle | null;
    disablePositioningStyle?: boolean;
};
declare function useRelativePosition({ align, avoidCollisions, triggerPosition, contentLayout, alignOffset, insets, sideOffset, side, disablePositioningStyle, }: UseRelativePositionArgs): ({
    readonly position: "absolute";
} & {
    top: number;
} & {
    left: number;
    maxWidth: number;
}) | {
    readonly position?: undefined;
    readonly opacity?: undefined;
    readonly top?: undefined;
    readonly zIndex?: undefined;
} | {
    readonly position: "absolute";
    readonly opacity: 0;
    readonly top: number;
    readonly zIndex: -9999999;
};
interface LayoutPosition {
    pageY: number;
    pageX: number;
    width: number;
    height: number;
}
interface GetPositionArgs {
    dimensions: ScaledSize;
    avoidCollisions: boolean;
    triggerPosition: LayoutPosition;
    contentLayout: LayoutRectangle;
    insets?: Insets;
}
interface GetSidePositionArgs extends GetPositionArgs {
    side: 'top' | 'bottom';
    sideOffset: number;
}
interface GetAlignPositionArgs extends GetPositionArgs {
    align: 'start' | 'center' | 'end';
    alignOffset: number;
}
type GetContentStyleArgs = GetPositionArgs & GetSidePositionArgs & GetAlignPositionArgs;

type UseControllableStateParams<T> = {
    prop?: T | undefined;
    defaultProp?: T | undefined;
    onChange?: (state: T) => void;
};
declare function useControllableState<T>({ prop, defaultProp, onChange, }: UseControllableStateParams<T>): readonly [T | undefined, React.Dispatch<React.SetStateAction<T | undefined>>];

declare function useIsomorphicLayoutEffect(effect: React.EffectCallback, dependencies?: React.DependencyList): void;

declare function useComposedRefs<T>(...refs: Array<React.Ref<T> | undefined>): React.RefCallback<T>;

/**
 * Stable callback that always calls the latest version of `callback`.
 * Compatibility shim for `useEffectEvent`.
 */
declare function useEffectEvent<TArgs extends readonly unknown[], TResult>(callback: (...args: TArgs) => TResult): (...args: TArgs) => TResult;

export { type LayoutPosition, useAugmentedRef, useComposedRefs, useControllableState, useEffectEvent, useIsomorphicLayoutEffect, useRelativePosition };
