import * as React from 'react';
import { RootProps, RootRef } from './index.js';
import './separator';
import '@rn-primitives/types';

type RootComponentProps = RootProps & React.RefAttributes<RootRef>;
declare const Root: {
    ({ asChild, decorative, orientation, ref, ...props }: RootComponentProps): React.JSX.Element;
    displayName: string;
};

export { Root };
